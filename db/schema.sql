create table candidate
(
    id   serial primary key,
    name text
);
create table post
(
    id   serial primary key,
    name text
);
create table users
(
    id   serial primary key,
    name text
);